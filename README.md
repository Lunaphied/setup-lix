There's a lot of GitHub action definitions for installing Nix, but few/none for installing Lix.

Rather than adapt one of the existing ones (which wasn't immediately successful), I just chose to write my own.

It works well enough for my small CI tests. Open issues if anything specific is useful to you.
